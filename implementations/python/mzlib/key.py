from typing import NewType


class IdType(int):
    '''A thin wrapper around an :class:`int` to abstract the type
    of the "id" type of all the entities in :title-reference`mzSpecLib`.
    '''
    pass